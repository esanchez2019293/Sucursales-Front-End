import { ProductoProveedorPipe } from './producto-proveedor.pipe';

describe('ProductoProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
